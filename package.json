{
  "name": "stuo-simulator-proxy",
  "version": "1.0.0",
  "description": "Proxy que guarda a chave da Anthropic no servidor para o Simulador Stuo poder ser compartilhado sem expor chave de API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  }
}
